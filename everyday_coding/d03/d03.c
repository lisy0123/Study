#include <stdio.h>

void res(int num)
{
    int i = 0;
    if (i == 0)
        printf("\"");
    if ()
    if (i == num)
        printf("\"");
}

int main()
{
    int a;

    printf("Input: ");
    scanf("%d", &a);
    printf("Output: [");
    res(a);
    printf("]");
    return (0);
}
